---
import { ArrowRight, Wrench, Activity, Zap } from "lucide-react";
---

<section class="relative pt-40 pb-24 px-6 overflow-hidden bg-[#030303] min-h-[90vh] flex items-center justify-center border-b border-white/5">
  
  {/* --- FONDOS Y EFECTOS --- */}
  <div class="absolute inset-0 z-0">
    {/* Textura de Ruido (Film grain) */}
    <div class="absolute inset-0 bg-[url('https://grainy-gradients.vercel.app/noise.svg')] opacity-[0.04] mix-blend-overlay"></div>
    
    {/* Luces Ambientales "Neon Repair" */}
    <div class="absolute top-0 left-1/4 w-[600px] h-[600px] bg-purple-900/20 blur-[150px] rounded-full animate-pulse-slow pointer-events-none mix-blend-screen"></div>
    <div class="absolute bottom-0 right-1/4 w-[500px] h-[500px] bg-cyan-900/10 blur-[150px] rounded-full animate-pulse-slow delay-1000 pointer-events-none mix-blend-screen"></div>
    
    {/* Grid Técnico de Fondo */}
    <div class="absolute inset-0 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear-gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:40px_40px] [mask-image:radial-gradient(ellipse_60%_50%_at_50%_0%,#000_70%,transparent_100%)] pointer-events-none"></div>
  </div>

  <div class="max-w-5xl mx-auto text-center relative z-10">
    
    {/* Badge: Diagnóstico del Sistema */}
    <div class="inline-flex items-center gap-3 py-1.5 px-4 rounded-full bg-white/5 border border-white/10 text-purple-300 text-[10px] font-bold tracking-[0.2em] uppercase mb-10 backdrop-blur-md animate-fade-in-up">
      <div class="relative flex h-2 w-2">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-purple-400 opacity-75"></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-purple-500"></span>
      </div>
      <span class="flex items-center gap-2">
         <Wrench size={12} /> Diagnóstico: Recuperable
      </span>
    </div>
    
    {/* Título Principal */}
    <h1 class="text-5xl md:text-8xl font-black uppercase tracking-tighter mb-8 leading-[0.9] text-white animate-fade-in-up delay-100">
      Tu web no está muerta. <br/>
      <span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-400 via-pink-400 to-cyan-400 animate-gradient bg-300%">Solo necesita un motor nuevo.</span>
    </h1>
    
    {/* Subtítulo / Propuesta de Valor */}
    <p class="text-xl md:text-2xl text-gray-400 max-w-3xl mx-auto mb-12 leading-relaxed font-light animate-fade-in-up delay-200 border-l-2 border-white/10 pl-6 md:border-none md:pl-0">
      Tu sitio actual ya tiene marca y contenido, pero el código viejo la está matando. 
      <strong class="text-white block md:inline mt-2 md:mt-0"> Hacemos un trasplante de corazón: misma apariencia, tecnología moderna.</strong>
    </p>

    {/* Botones de Acción */}
    <div class="flex flex-col sm:flex-row gap-6 justify-center items-center animate-fade-in-up delay-300">
      <a href="#pricing" class="group relative px-8 py-4 bg-purple-600 text-white font-bold rounded-full transition-all hover:scale-105 hover:bg-purple-500 shadow-[0_0_40px_-10px_rgba(168,85,247,0.5)] overflow-hidden">
         <div class="absolute inset-0 bg-white/20 translate-y-full group-hover:translate-y-0 transition-transform duration-300"></div>
         <span class="relative flex items-center gap-2">
           Ver Planes de Rescate <ArrowRight size={20} className="group-hover:translate-x-1 transition-transform" />
         </span>
      </a>
      
      <a href="#proceso" class="group flex items-center gap-2 text-sm font-bold text-gray-400 hover:text-white transition-colors border-b border-transparent hover:border-white pb-0.5">
         <Activity size={16} className="text-cyan-400 group-hover:animate-pulse" />
         ¿Cómo funciona el trasplante?
      </a>
    </div>

    {/* Métricas de Impacto (Footer del Hero) */}
    <div class="mt-20 pt-10 border-t border-white/5 flex flex-wrap justify-center gap-12 md:gap-24 text-center animate-fade-in-up delay-500 opacity-60 hover:opacity-100 transition-opacity duration-500">
        <div class="group">
           <div class="flex justify-center mb-2 text-purple-500 group-hover:scale-110 transition-transform"><Zap size={24} /></div>
           <div class="text-3xl font-black text-white">+300%</div>
           <div class="text-[10px] text-gray-500 uppercase tracking-widest font-bold">Velocidad</div>
        </div>
        <div class="group">
           <div class="flex justify-center mb-2 text-cyan-500 group-hover:scale-110 transition-transform"><Activity size={24} /></div>
           <div class="text-3xl font-black text-white">99/100</div>
           <div class="text-[10px] text-gray-500 uppercase tracking-widest font-bold">Google Score</div>
        </div>
        <div class="group">
           <div class="flex justify-center mb-2 text-green-500 group-hover:scale-110 transition-transform"><Wrench size={24} /></div>
           <div class="text-3xl font-black text-white">Zero</div>
           <div class="text-[10px] text-gray-500 uppercase tracking-widest font-bold">Plugins</div>
        </div>
    </div>

  </div>
</section>

<style>
  /* Animación de Gradiente de Texto */
  @keyframes gradient {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  .animate-gradient {
    background-size: 300%;
    animation: gradient 8s ease infinite;
  }

  /* Fade In Up Standard */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translate3d(0, 40px, 0);
    }
    to {
      opacity: 1;
      transform: translate3d(0, 0, 0);
    }
  }
  .animate-fade-in-up {
    animation: fadeInUp 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
    opacity: 0; 
  }
  .delay-100 { animation-delay: 0.1s; }
  .delay-200 { animation-delay: 0.2s; }
  .delay-300 { animation-delay: 0.3s; }
  .delay-500 { animation-delay: 0.5s; }

  /* Pulso Lento Ambiental */
  @keyframes pulseSlow {
    0%, 100% { opacity: 0.2; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(1.1); }
  }
  .animate-pulse-slow {
    animation: pulseSlow 6s ease-in-out infinite;
  }
</style>