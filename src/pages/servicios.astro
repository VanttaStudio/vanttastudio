---
import BaseLayout from "../layouts/BaseLayout.astro";
import Footer from "../components/global/Footer.astro";
import { ServicesBento } from "../components/services/ServicesBento";
import { Sparkles } from "lucide-react";
---

<BaseLayout title="Servicios | VanttaCode Ecosistema">
  
  <main class="bg-black min-h-screen w-full relative flex flex-col overflow-x-hidden selection:bg-purple-500/30">
    
    {/* FONDO AMBIENTAL */}
    <div class="absolute inset-0 z-0 pointer-events-none">
      <div class="absolute inset-0 bg-[url('/grid-pattern.svg')] opacity-[0.05]"></div>
      {/* Luz superior */}
      <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[80vw] h-[500px] bg-purple-900/20 blur-[120px] rounded-full opacity-50"></div>
      {/* Luz inferior */}
      <div class="absolute bottom-0 right-0 w-[500px] h-[500px] bg-blue-900/10 blur-[150px] rounded-full opacity-30"></div>
    </div>

    {/* HEADER */}
    <div class="relative z-10 pt-32 px-6 md:px-10 mb-12 text-center md:text-left max-w-7xl mx-auto w-full">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-white/5 border border-white/10 text-[10px] font-bold text-gray-400 uppercase tracking-[0.2em] mb-6 backdrop-blur-md">
         <Sparkles size={12} className="text-purple-400" /> Ecosistema Vantta
      </div>
      
      <h1 class="text-white text-5xl md:text-7xl font-black uppercase tracking-tighter leading-[0.9]">
        Selecciona tu <br/>
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-400 via-white to-purple-400 animate-gradient bg-300%">Misión</span>
      </h1>
      
      <p class="mt-6 text-gray-400 max-w-xl text-lg font-light leading-relaxed border-l-2 border-white/10 pl-6 hidden md:block">
        Desde desarrollo web de alto impacto hasta sistemas operativos autónomos. Todo lo que tu negocio necesita para escalar.
      </p>
    </div>

    {/* BENTO GRID */}
    <div class="relative z-10 flex-1 px-4 md:px-6 pb-20 max-w-7xl mx-auto w-full">
      <ServicesBento client:visible />
    </div>

  </main>

  <Footer />

</BaseLayout>

<style>
  @keyframes gradient {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  .animate-gradient {
    background-size: 300%;
    animation: gradient 8s ease infinite;
  }
</style>